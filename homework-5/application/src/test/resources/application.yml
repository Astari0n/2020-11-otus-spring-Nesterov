spring:
  datasource:
    initialization-mode: always
    schema: test-schema.sql
    data: test-data.sql

    url: jdbc:h2:mem:testdb

  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true

  shell:
    interactive:
      enabled: false